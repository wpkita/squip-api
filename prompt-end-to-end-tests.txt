Create a C# Console application called Squip.Rest.Verify that performs end-to-end tests against the existing Squip.Rest Controller endpoints (via HTTP requests). Do NOT reference any of the code in your implementation. First, write an end-to-end test for every endpoint individually. Second, write end-to-end tests for each controller such that POST, GET (what was just POSTed), PUT (update what was POSTed), GET (verify that it was updated via PUT), DELETE (the one that was created and updated), and finally GET again (to verify that the one created earlier was deleted). If all tests succeed, print "SUCCESS". Otherwise, print "FAILURE". Do you need any clarification?
